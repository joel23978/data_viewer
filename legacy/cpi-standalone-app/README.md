Legacy standalone CPI app files moved out of the active app root.

These files are retained for reference only.

They are not part of the current main application boot path.

Archived files:
- `cpi_app.R`
- `R/cpi_app.R`
- `cpi_monthly.R`
- `cpi_annual_update_data.R`
